name: Tests
on: pull_request

jobs:
  build:
    name: PHPUnit
    runs-on: sigere/order-manager-dev
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v1

      - name: Unit
        run: |
          composer install --no-interaction
          php ./bin/phpunit