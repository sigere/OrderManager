{% extends 'base.html.twig' %}

{% block title %}Menedżer Zleceń{% endblock %}

{% block body %}
<style>
    body{
        font-size: 20px;
        color: white;
        background-color: rgb(20,20,20);
    }
</style>
    <table>
    <tbody>
    <tr>
        
    {# {% for o in order %}
        <td>{{ o }}</td>
    {% endfor %} #}
    </tr>
    <tbody>
    </table>
    {% for l in logs %}
        {{ l.time }}
        {{ l.order }}
    {% endfor %}
    <br/>
    <h3>Logi:</h3>
    <table>
        {% for l in logs %}
            <td>{{ l.id }}</td>
            <td>{{ l.user }}</td>
            <td>{{ l.time }}</td>
            <td>{{ l.action }}</td>
            <td>{{ l.order }}</td>
        {% endfor %}
    </table>
{% endblock %}
