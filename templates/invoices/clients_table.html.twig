<table class="table table-dark table-hover">
    <thead>
    <tr>
        <th>id</th>
        <th>Alias</th>
        <th>Pełna nazwa</th>
        <th>Zlecenia</th>
    </tr>
    </thead>
    <tbody>
    {% for client in clients %}
        <tr data-subject-id="{{ client['client'].id }}" data-subject-type="client">
            <td class="clickable-row" style="white-space: nowrap;">
                {{ client['client'].id }}
            </td>
            <td class="clickable-row">{{ client['client'].alias }}</td>
            <td class="clickable-row">{{ client['client'].name }}</td>
            <td class="clickable-row">{{ client['count'] }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
