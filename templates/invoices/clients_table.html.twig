<table class="table table-dark table-hover">
    <thead>
    <tr>
        <th>id</th>
        <th>Alias</th>
        <th>Pełna nazwa</th>
        <th>Zlecenia</th>
    </tr>
    </thead>
    <tbody>
    {% for client in clients %}
        <tr data-subject-id="{{ client[0].id }}" data-subject-type="client">
            <td class="clickable-row" style="white-space: nowrap;">
                {{ client[0].id }}
            </td>
            <td class="clickable-row">{{ client[0].alias }}</td>
            <td class="clickable-row">{{ client[0].name }}</td>
            <td class="clickable-row">{{ client[1] }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
