<table id="reports-table" class="table table-dark table-hover">
    <thead>
    <tr>

    </tr>
    </thead>
    <tbody>
    {% for report in reports %}
        {% dump(reports) %}
        <tr>
            <td class="clickable-row" report-id="{{ report['id'] }}">
                {{ report['name'] }}
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>