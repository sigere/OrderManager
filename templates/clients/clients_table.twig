<table id="main-table" class="table table-dark table-hover">
    <thead>
    <tr>
        <th>id</th>
        <th>Alias</th>
        <th>Pełna nazwa</th>
        <th>NIP</th>
{#        <th>Adres e-mail</th>#}
    </tr>
    </thead>
    <tbody>
    {% for client in clients %}
        <tr client-id="{{ client.id }}">
            <td class="clickable-row" style="white-space: nowrap;">
                {% if not client.email %}
                    {% include 'icons/warning.html'  %}
                {% endif %}
                {{ client.id }}
            </td>
            <td class="clickable-row">{{ client.alias }}</td>
            <td class="clickable-row">{{ client.name }}</td>
            <td class="clickable-row">{{ client.nip }}</td>
            <td class="clickable-row">{{ client.email|default("brak") }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
