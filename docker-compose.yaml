services:
   php:
      build:
         context: ./.docker/php
      volumes:
         - .:/var/www/OrderManager
      ports:
         - '8000:8000'
         - '443:443'

   node:
      image: node:20.15.0
      volumes:
         - .:/var/www/OrderManager